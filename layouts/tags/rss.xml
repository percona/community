{{- $contributor := false -}}
{{- $tag := lower .Title -}}
{{- range where site.RegularPages "Section" "contributors" -}}
  {{- if eq (lower .Params.planet_pg_tag) $tag -}}
    {{- $contributor = . -}}
  {{- end -}}
{{- end -}}
{{- $pages := sort .Pages ".Params.date" "desc" -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = $pages | first $limit -}}
{{- end -}}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    {{- with $contributor -}}
    <title>{{ with .Params.planet_pg_feed_title }}{{ . }}{{ else }}{{ .Params.fullname }} — Percona Community{{ end }}</title>
    <link>{{ $.Permalink }}</link>
    {{- $feedDesc := "" -}}
    {{- with .Params.planet_pg_feed_description -}}
      {{- $feedDesc = . -}}
    {{- else -}}
      {{- $feedDesc = printf "PostgreSQL articles and insights by %s on the Percona Community blog" .Params.fullname -}}
    {{- end -}}
    <description>{{ $feedDesc }}</description>
    {{- else -}}
    <title>{{ .Title }} on {{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content in {{ .Title }} on {{ .Site.Title }}</description>
    {{- end -}}
    <generator>Hugo {{ hugo.Version }}</generator>
    <language>{{ site.Language.Locale }}</language>
    {{- with $pages -}}
    <lastBuildDate>{{ (index $pages 0).Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end -}}
    {{- with .OutputFormats.Get "RSS" -}}
    <atom:link href="{{ .Permalink }}" rel="self" type="application/rss+xml"/>
    {{- end -}}
    {{- range $pages -}}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with site.Params.author -}}
      {{- with .email -}}
      <author>{{ . }}{{ with $.Site.Params.author.name }} ({{ . }}){{ end }}</author>
      {{- end -}}
      {{- end -}}
      <guid isPermaLink="true">{{ .Permalink }}</guid>
      <description>{{ .Summary | html }}</description>
    </item>
    {{- end -}}
  </channel>
</rss>
